# 한화에몽 MarketView — 매일 아침 데이터 갱신 작업

너는 `C:\Users\user\Desktop\scratchpad\hanwha-emong-draft.html` (단일 파일 대시보드)의
**시장 데이터만** 매일 최신으로 갱신하는 작업을 수행한다.

## 절대 규칙
1. **디자인·레이아웃·구성·JS 함수는 절대 바꾸지 않는다.** 오직 데이터 값, spark 배열,
   날짜/기준일자 문자열, 인사이트 문장, disc 출처 문구만 갱신한다.
2. 기준일자 = 환경변수 `HE_ASOF` (예: `2026.09.04`). 없으면 오늘 날짜(KST).
   - 헤더의 `기준 YYYY.MM.DD ...`, 홈 히어로 하단 문구, footer의 `기준 YYYY.MM.DD.` 를 모두 이 값으로.
   - 미국장은 직전 거래일 종가 기준임을 문구에 반영(휴장일 고려).
3. **무료·무키 데이터만** 사용. 유료 API 금지. 가능한 소스:
   - 지수·원자재·환율·미 국채(30Y/10Y/5Y): Yahoo Finance chart API
     `https://query1.finance.yahoo.com/v8/finance/chart/{SYMBOL}?range=1mo&interval=1d`
     (WebFetch로 JSON 취득. 심볼: ^KS11 ^KQ11 ^IXIC ^GSPC ^DJI ^N225 000001.SS ^STOXX50E
     CL=F BZ=F GC=F SI=F HG=F NG=F KRW=X DX-Y.NYB EURUSD=X JPY=X CNY=X ^TYX ^TNX ^FVX)
   - 미 국채 2년: `https://home.treasury.gov/resource-center/data-chart-center/interest-rates`
     Daily Treasury Par Yield 페이지 또는 FRED `DGS2` 페이지(무료 열람).
   - **P1(지수·지표)의 채권 금리 카드는 미 국채 30/10/5Y 3개만 유지한다.** 국내 금리·미 2Y 등
     Yahoo Finance로 실측 불가한 항목은 P1에 다시 추가하지 않는다(예시/mock 카드 금지).
   - 증시 캘린더 날짜: Fed FOMC 캘린더, BLS 릴리스 캘린더(공식 페이지). 확정 일정만 갱신,
     추정 항목은 `mock`/`예시` 태그 유지.
   - 전일 수급(P4): 한국경제·머니투데이·인포스탁데일리·연합인포맥스 등 당일 마감 기사 검색.
     주체별 순매수/순매도 금액과 방향을 기사 종합으로 갱신. 종목별 정량 순위가 없으면
     기존 mock 유지 + disc에 명시.
   - 중장기(P5): 공개 증권사 리포트·기사에서 목표주가·투자의견 갱신. 없으면 mock 유지.
4. spark 배열은 **최근 약 1개월 일별 종가**로 재구성(길이 18~22 유지). 소수 자릿수는
   기존 항목과 동일하게. `val`은 최신 종가, `chg`/`pct`는 전일 대비.
5. 값을 실제로 확인하지 못한 항목은 **임의로 지어내지 말 것**. 기존 값 유지하고 해당
   카드의 `mock:true` 및 disc 문구를 유지/보강한다.
6. 인사이트(각 탭 상단 `.insight`)는 그날 데이터에 맞게 2~4줄로 갱신하되 **명사형 어미**로
   끝맺는다(예: `…압박`, `…관건`, `…축소`). 종결형(`…한다/…이다`) 사용 금지.

## 작업 순서
1. `HE_ASOF` 확인. 파일 읽기.
2. Yahoo Finance로 지수/원자재/환율/미국채(30·10·5Y) 최신 1개월 시계열 취득 → IDX/COM/FX/RATE 갱신.
3. 미 2Y·국내 금리(국고 3/10Y·회사채·CD) 웹 검색으로 확인 → 확인분만 갱신, `mock:true`는 확인 시 제거.
4. P2 캘린더: 확정 일정 날짜/결과값 갱신.
5. P4 수급: 최신 마감 기사 종합으로 방향·금액 갱신.
6. P5: 공개 리포트로 목표주가·현재가 갱신.
7. 각 탭 인사이트 문구를 당일 상황으로 재작성(명사형 어미).
8. 기준일자 문자열 3곳 + disc 날짜 갱신.
9. 저장. HTML 구조가 깨지지 않았는지 확인(태그 균형, `<script>` 문법).
10. 한 줄 요약(무엇을 갱신했고 무엇이 여전히 mock인지) 출력.

## 하지 말 것
- 새 탭/섹션/CSS 규칙 추가
- 함수 시그니처 변경
- 확인 안 된 수치 창작
- 파일 경로 변경 / 새 파일 생성(로그 제외)
